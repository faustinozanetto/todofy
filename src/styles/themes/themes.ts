import { createTheme } from '@nextui-org/react';

const lightTheme = createTheme({
  type: 'light',
});

const darkTheme = createTheme({
  type: 'dark',
});

export const themes = {
  lightTheme,
  darkTheme,
};
